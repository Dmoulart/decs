{
    "name": "decs",
    "version": "1.0.0",
    "license": "MIT",
    "dependencies": {},
    "type": "module",
    "main": "./dist/index.js",
    "files": [
        "./dist/**/*"
    ],
    "author": "dmoulart",
    "scripts": {
        "clean": "rm -rf ./dist",
        "build": "tsc -p tsconfig.json",
        "bench:node": "node  --experimental-specifier-resolution=node ./dist/bench/ecs.js",
        "bench:bun": "bun run ./dist/bench/ecs.js",
        "test": "jest --coverage ./test",
        "test:watch": "jest --watchAll",
        "prepare": "yarn clean && yarn build && yarn test"
    },
    "devDependencies": {
        "@types/jest": "^29.2.4",
        "@types/node": "^18.11.16",
        "chalk": "^5.2.0",
        "jest": "^29.3.1",
        "ts-jest": "^29.0.3",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.4"
    }
}
